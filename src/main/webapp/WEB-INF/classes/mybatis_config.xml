<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="mybatis_db.properties"></properties>
    <settings>
        <setting name="logImpl" value="LOG4J"/>
        <!--是否开启自动驼峰命名规则（camel case）映射，即从经典数据库列名 A_COLUMN 到经典Java 属性名 aColumn 的类似映射。 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
        <package name="system"/>
        <package name="mapper"/>
    </typeAliases>
    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
<!--    <mappers>-->
<!--&lt;!&ndash;        <mapper resource="BookDaoMapper.xml"/>&ndash;&gt;-->
<!--        <package name="com.share.charge.mybatis.generator.mapper"/>-->
<!--        <package name="mapper"/>-->
<!--        &lt;!&ndash;        <mapper resource="system/SystemBooksInfoMapper.xml"/>&ndash;&gt;-->
<!--    </mappers>-->
</configuration>
